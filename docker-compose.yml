---
services:
  assembly:
    build:
      context: assembly/
    volumes:
      - share:/mnt/share

  production:
    build:
      context: production/
    volumes:
      - share:/mnt/share
    ports:
       - "8080:8080"  
    depends_on:
      - assembly
volumes:
  share: 